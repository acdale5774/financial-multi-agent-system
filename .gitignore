# Environment / secrets — never commit real credentials.
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# SimFin downloaded bulk-dataset cache
_simfin_cache/
**/_simfin_cache/

# Case-study source corpus (294MB of SEC filings HTML + transcripts) — too
# large for git; obtain separately and place at the repo root.
case_study_data/

# OS / editor
.DS_Store
.idea/
.vscode/
